name: 'audit_helper'
version: '0.4.0'
config-version: 2

require-dbt-version: [">=1.2.0", "<2.0.0"]

profile: joel_ska

target-path: "target"
clean-targets: ["target", "dbt_packages"]
macro-paths: ["macros"]
log-path: "logs"
